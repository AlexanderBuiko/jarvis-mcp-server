# Pinned to exact versions for reproducible Cloud Run builds — the image installs
# the same packages tested locally (no surprise SDK upgrades on a future redeploy).

# Official MCP SDK (server + Streamable HTTP transport).
mcp==1.28.0
# ASGI server + framework. We build/serve the app ourselves (auth middleware +
# uvicorn), so these are pinned explicitly, not left to mcp's transitive deps.
uvicorn==0.49.0
starlette==1.3.1
# CA bundle so HTTPS to the public APIs works on macOS python.org builds.
certifi==2026.6.17

# The RAG "brain" for the /help (and, later, /review) endpoints reuses jarvis-cli's
# indexing / rag / llm-gateway primitives rather than duplicating them. Installed
# separately (not pinned here) because it lives in the sibling repo:
#   local dev:  pip install -e ../jarvis-cli
#   Cloud Run:  pip install "jarvis-cli @ git+https://…@<pinned-sha>"  (or vendored)
# Pulls transitive CLI-only deps (prompt-toolkit, plotext); a slim jarvis-core
# extract can drop those later.
